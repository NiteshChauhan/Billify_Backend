Local PDF fonts used by the multilingual HTML-to-PDF renderer.

Expected files:

- `NotoSans-Regular.ttf`
- `NotoNaskhArabic-Regular.ttf`
- `NotoSansDevanagari-Regular.ttf`

These fonts are embedded into generated PDFs so Arabic, Hindi, and English
render consistently across systems without depending on OS-installed fonts.
